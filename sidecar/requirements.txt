# Full dependency set: base (shared with CI) + ML stack + packaging.
# CI installs requirements-base.txt only — keep ML/packaging deps below.
-r requirements-base.txt
insightface>=1.0.1
onnxruntime>=1.18.0
pyinstaller>=6.7.0
