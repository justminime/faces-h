# Core sidecar dependencies — everything except the heavyweight ML stack and
# packaging tools. CI installs THIS file (the ML models can't run headless in
# CI anyway); requirements.txt includes it so local/dev/release installs get
# the full set. Add new runtime deps here unless they are ML/packaging-only.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0
faiss-cpu>=1.8.0
Pillow>=10.3.0
pillow-heif>=0.16.0
piexif>=1.1.3
numpy>=1.26.0
psutil>=5.9.0
ruff>=0.4.0
mypy>=1.10.0
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
