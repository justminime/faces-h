{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for faces-h",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
